#ifndef(ADINF_INCLUDE_RENDER_TRIANGLE)
#define ADINF_INCLUDE_RENDER_TRIANGLE

typedef struct render_triangle_t {
  float x_coord; // X coordinate of the rendering triangle
  float y_coord; // Y coordinate of the rendering triangle
  float o_coord; // origin coord of the rendering triangle
  float weight;  // weight value of the rendering triangle

} render_triangle;


#endif
